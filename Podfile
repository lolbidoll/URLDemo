# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NativeUrl' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Smartech-iOS-SDK', '~> 3.4.1'
  pod 'SmartPush-iOS-SDK', '~> 3.4.1'

  # Pods for NativeUrl

  target 'NativeUrlTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NativeUrlUITests' do
    # Pods for testing
  end

end
